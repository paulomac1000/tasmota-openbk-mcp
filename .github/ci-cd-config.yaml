# CI/CD Configuration Contract - local-home-devices-mcp
language: "python"
src_dir: "tools/"
package: "tools"
project: "local-home-devices-mcp"
python_version: "3.14"
image_name: "local-home-devices-mcp:test"
smoke_name: "mcp-smoke"
health_port: 9100
rest_port: 9102
use_docker: true
is_mcp: true
expected_tools: 67  # v1.6.0 — 51 base + 16 new config tools
use_codecov: true
use_semgrep: true
use_dependabot: true
package_ecosystems: ["github-actions", "pip", "docker"]
use_docs_validation: false

services:
  mosquitto:
    image: eclipse-mosquitto:latest
    ports:
      - "1883:1883"
